{
  "name": "@anthropic-apex/oracle-apex-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for Oracle Database + Oracle APEX 20.2 integration — queries, DDL/DML, transactions, object inspection, and APEX REST API read-only access",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "oracle-apex-mcp-server": "dist/index.js"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "oracledb": "^6.8.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
